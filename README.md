## Matrix Strategy

This GH-repository contains a workflow sample that demonstrates
the use of the Matrix-strategy in order to RUN 'multiple-variants'
of a 'base-JOB' using different configurations (by default ALL POSIBLE
COMBINATIONTS of the variables values defined in the matrix).